<!DOCTYPE html>
<html>
  <head>
    <title>PoteShare</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <header class='pote-share'>
      <div class='title-search-era'>
        <h3 class="title">Pote Share</h3>
      </div>
      <div class='user'>
        <% if user_signed_in? %>
          <% if current_user.image? %>
            <%= image_tag current_user.image.thumb.url, class: 'icon' %>
            <%= current_user.name %>
          <% else %>
            <%= image_tag "/assets/default1.jpg", class: 'icon', :size => '100x100'  %>
          <% end %>
        <% else %>
          <h3><%= button_to "ログイン", :new_user_session, method: :get %></h3>
          <h3><%= button_to "登録する", :new_user_registration, method: :get %></h3>
        <% end %>
        <h3><%= link_to "ログアウト", :destroy_user_session, method: :delete  %></h3>
      </div>
    </header>

    <%= yield %>

    <footer class='pote-share-footer'>
      <p><small>&copy; 2021 POTEPAN</small></p>
    </footer>
  </body>
</html>
